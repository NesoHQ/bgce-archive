name: Manual Build and Push - Archive Admin

on:
    workflow_dispatch:
        inputs:
            image-tag:
                description: "Docker image tag"
                required: false
                type: string
                default: "latest"

env:
    SERVICE_NAME: archive-admin

jobs:
    build-and-push:
        permissions:
            contents: read
            packages: write
        uses: ./.github/workflows/docker-build.yml
        with:
            service: archive-admin
            should-build: true
        secrets: inherit
